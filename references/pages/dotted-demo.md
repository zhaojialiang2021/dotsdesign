---
name: 点点demo
status: draft
last_updated: 2026-07-08
used_by: [docs-pages, patterns]
---

# 点点demo

## 需求背景

点点demo 是 Dots 独立设计系统的页面样本，用来验证「Figma 规范 → 规格文档 → 页面 demo → GitHub → Vercel 预览」这条流程能否跑通。

当前视觉真相源切换为 Figma `think 方案` 节点 `3:4417`。旧版 `Dots 规范` 中的快捷提示、系统键盘、Skill 选中态和护肤 AI 卡片不再作为当前对话页框架基座；如果后续要恢复这些能力，必须先重新进入组件规格流程，而不是在页面 demo 私自拼回旧结构。

## 页面结构

1. iOS 状态栏：时间 9:41，右侧信号、Wi-Fi、电池。
2. 顶部导航：左侧返回，头像 + 标题「点点」，右侧更多。
3. 对话区：真实消息流容器，复用 `DotsMessageBubble`；项目 demo 不展示默认问候气泡。
4. 底部输入区：玻璃渐隐底座 + 48px 输入框。
5. 输入框内容：左侧语音图标，占位文案「发消息或按住说话...」，右侧相机和添加。
6. 底部标注：`内容由AI生成` + iOS home indicator。

## 视觉规格

- 画板为 393×852px，外壳圆角 48px，使用 iOS 连续圆角 / squircle 裁切，外部 8px 纯白色外描边，不占用内部画布尺寸，背景为 `Bg0` / `#F5F5F5`。
- 顶部基座高 110px：系统状态栏 54px，导航栏 56px，背景与页面背景一致。
- 导航栏左右 padding 16px；返回、更多图标均为 24×24px；头像为 24×24px，圆角 8px；标题 16px / 24px / Medium。
- 对话区从 y=110px 开始一直延伸到页面底部，左右 padding 16px，隐藏滚动条并保留触控惯性滚动；底部通过 128px 内边距给输入区和渐隐遮罩预留可读空间，内容允许进入底部渐隐区域但不能被输入框遮挡。
- 项目 demo 顶部不展示「今天想问点什么～」默认问候气泡，用户消息作为当前演示对话的第一条可见消息。
- 用户气泡右对齐，宽 293px，背景 `rgba(0,0,0,0.08)`，0.5px 白色弱描边，圆角 18px，padding 12px 16px，文字 16px / 28px；气泡内有两条 0.75px 虚线，dash 2px / gap 2px，第一条位于 y=40、宽 261px，第二条位于 y=68、宽 80px。
- 回答判断态第一步参考 Figma `think 方案` node `7:535`：用户消息发送后，先在 Dots 左侧展示 `cloud.json` 判断 Lottie，而不是普通 loading。判断态外框尺寸 52×50px，背景位于 x=-10 / y=-2，尺寸 72×54px；cloud 图标 36×36px，位于外框内 x=8 / y=7；正文隐藏，不显示可见文字。
- 回答判断态第二步参考 Figma `7月方案` node `187:4125`：cloud 动画、脑雾背景和简单判断文案同步开始，不需要先停顿。cloud 图标位置和第一步完全一致，仍位于外框内 x=8 / y=7，避免状态切换跳闪。脑雾外框从 52×50px 跟随文字输出进度横向展开，最终为 263×50px；背景位于 x=-10 / y=-2，最终尺寸 283×54px；正文位于 x=44 / y=13，最大宽 211px，高 24px，文字颜色与后续思考过程标题一致，文案为「正在规划新疆伊犁环线10天行程」，按字符流式输出。扫光只在文案完整输出后触发，使用低对比、慢速横向扫光。
- 简单判断和 context 回复必须连接为一个连续动作：简单判断文案完成后保持脑雾 2s，再自动替换为 Dots 回复气泡；中间不需要用户再次点击，也不新增独立 loading 消息。context 回复气泡使用轻量入场，位移控制在 6px 内，420ms 内完成淡入；文案使用快速字符流式输出，每次追加 2 个字符，约 16ms 一步，输出完成后短暂停留再承接后续 think，避免直接闪现造成生硬感。
- 正式回复内容参考 Figma `7月方案` node `187:4125`：`暑期伊犁环线是个很经典的选择，我先帮你把路线框架、核心景点、住宿交通这些关键信息都搜集好，整理出一份适合六人出行的攻略。`
- think 过程参考 Figma `7月方案` node `187:4125`：正式回复完成后，在下一条 Dots 消息位置追加思考脑雾。容器位于对话区 16px padding 内，最终宽 361px；背景相对容器外扩 left/right 10px、top/bottom 6px，视觉范围为页面 x=6 到 x=387，圆角 22px，blur 8px，透明度 90%。左上图标 36×36px，位于容器内 x=8 / y=7。标题位于容器内 x=44 / y=13，最大宽 273px，14px / 24px，文案为「规划新疆伊犁红圈10天行程」；正文位于容器内 x=16 / y=47，最大宽 329px，12px / 18px，页面左右边距均为 32px，必须允许正常换行，文案为「用户需要计划去新疆伊犁的10天旅行，和5个朋友一起，考虑到预算没有给限制，住宿推荐4星级，出行推荐自驾游，可以欣赏风景和自由摄影。」标题和正文都按字符流式输出，只有标题在完整输出后触发慢速扫光；脑雾内部必须使用正常文档流布局，正文和检索结果作为容器内容自然撑高，不能用 JS 估算行数、绝对定位或固定高度裁切内容。
- toolcall 过程参考 Figma `7月方案` node `168:2894`：think 完成后同一条脑雾渐变切换到 toolcall，动画使用 `pen.json`。标题文案为「制定新疆伊犁环线10日自驾游路线」；正文使用原第二段 toolcall 的检索内容，依次展示「网页｜那拉提旅游风景区官方网站」「网页｜喀拉峻国际生态旅游区官方信息」「网页｜夏塔景区开放时间与票务信息」「网页｜赛里木湖景区游客服务信息」「更多官方信息检索中...」。标题和正文都按字符流式输出，只有标题在完整输出后扫光。
- 第二段 think 过程参考 Figma `7月方案` node `168:2894`：toolcall 完成后同一条脑雾渐变切回 think，动画回到 `cloud.json`。容器固定使用常规 361px 宽，不再收窄为紧凑单行；标题文案为「查找景区开放信息和路线攻略」，正文文案为「我需要更多官方信息，特别是那拉提景区、喀拉峻景区、夏塔景区和赛里木湖的开放时间和票价信息。虽然目前的资料可能会变动，但我还是可以提供建议。」标题和正文都按字符流式输出；正文输出完成后 cloud Lottie 继续播放 2s，再收起为完成态并进入下一项。
- 第二段 toolcall 过程参考 Figma `7月方案` node `168:2894`：第二段 think 完成后同一条脑雾渐变切换到 toolcall，动画使用 `glass.json`。标题文案为「搜索景区官方网站及相关信息」。该阶段不展示正文内容，不渲染胶囊、检索行或详情区域；标题完成后直接进入 toolcall hold，再承接下一段 think。
- 第三段 think 过程参考 Figma `7月方案` node `168:2894`：检索 toolcall 完成后同一条脑雾渐变切回 think，动画使用 `cloud.json`。标题文案为「考虑提供合理的行车距离和交通建议」；正文文案为「行车时间可以根据常见自驾估算，最终以当天导航为准。至于交通，6人客户可以考虑选择一辆较大的7座商务车或1+1车型来适应行李的需求。从乌鲁木齐进出新疆是比较方便的方式。」标题和正文都按字符流式输出，只有标题在完整输出后扫光。
- response 过程参考 Figma `think 方案` node `7:717` 与完成态 node `157:6748`：第三段 think 完成后停顿 1s，先进入 600ms 的思考完成确认态，最后一条 think 保留原位置并切换为完成对勾；随后思考脑雾整体收束为 response 摘要卡片。摘要态必须和正式 response 使用同一个卡片壳、同一套 `padding 20px 16px` 和内部间距，只显示顶部状态「思考完成，参考小红书与全网内容」；转场用 680ms 从脑雾式 blur 8px 逐渐清晰到白色 response 背景，不做位移，不切换摘要/正文两套布局，避免头部文案跳变。摘要停留约 420ms 后，在同一卡片壳内开始正文字符流式输出，卡片沿正常文档流向下拓展。response 卡片不做位移，只从 0.98 透明度柔和淡入，时长 900ms。卡片不能吸底，正文增长时不能把已出现回答持续往上顶。卡片宽 361px，白底，0.5px `Separator` 描边，圆角 22px；顶部状态 14px / 20px，颜色 `Description`，右侧保留叠头像和箭头。正文整体 16px / 28px，section 之间 gap 20px；section 标题 16px / 24px Semibold，标题与正文 gap 14px；正文内容严格参考 Figma `7月方案` node `157:6748`：先输出 10 天伊犁环线总结，再依次输出「🚗 行程安排」「📷 沿途景点」「✨ 实用提醒」三个模块；本版 response 不使用 bullet 列表。
- response 卡片必须使用设计稿里的图片资源：状态行头像叠放、右箭头、底部复制/分享/不喜欢/刷新图标都必须是真实切图，不允许用文本符号占位。底部复制/分享/不喜欢/刷新操作栏只在 response 正文完整流式输出结束后展示，不能在流式输出过程中吸附在卡片底部。
- response 顶部状态行点击后参考 Figma `think 方案` node `8:1598` 打开思考/来源半层。半层在当前 393×852 画板内从底部拉起进入，使用 `translateY(100%) -> 0` 的 420ms 柔和缓动 `cubic-bezier(0.16, 0.84, 0.32, 1)`；遮罩同步淡入。半层高 639px，背景 `Bg0 Lighter / #FAFAFA`，顶部圆角 38px，阴影 `0 15px 37.5px rgba(0,0,0,0.18)`；背景页面加 20% 黑色遮罩。顶部 grabber 位于 x=178.5 / y=5，36×5px，圆角 100px，颜色 `#CCCCCC`。半层内容从 y=20 开始，左右 padding 12px，模块间距 20px。标题区为「思考过程」，padding `4px 8px 12px`，16px / 24px Medium。思考过程卡片白底、圆角 16px、padding 12px；每行左侧进度列宽 24px，状态圆 24×24px、`rgba(48,48,52,0.05)`，连接线为 1px 竖向虚线，dash 1.5px / gap 1.5px，颜色 `rgba(20,20,20,0.08)`；文本 13px / 20px Medium，右侧 16px chevron。参考来源模块标题为「参考来源 · 128」，下方筛选胶囊和 Source Item 卡片按设计稿展示；当来源列表滚动到笔记区域时，「参考来源 · 128」标题和筛选胶囊组成的导航需要吸顶在半层滚动容器顶部，背景同半层 `#FAFAFA`，防止内容穿透。卡片白底、圆角 16px、padding `12px 12px 16px`，图片 3 列、gap 6px、圆角 6px。
- 半层里的思考过程文字色不能沿用对话流脑雾的 `Description` 弱层级；标题使用 `Title` / 13px / 20px Medium，正文、检索行、网页行和更多入口使用 `Paragraph` 层级。`搜索景区官方网站及相关信息` 展开内容中的网页来源标题严格按 Figma node `168:2894`：`PingFang SC Regular`、12px / 18px、字距 0.24px、单行展示，超出一行必须用 ellipsis 截断。
- 思考过程中，正在展示的思考脑雾整体都可点击，点击后参考 Figma `think 方案` node `8:1257` 从底部拉起「思考中」半层。半层复用同一套 639px 底部拉起、20% 遮罩、grabber、白底过程卡片和 24×24 状态列；标题为「思考中」，如果半层保持打开且主流程已经进入 response / complete，标题自动更新为「思考结束」。内容只展示当前已经走到的 think/toolcall 链路，数据必须来自前面对话里的流式过程；状态圆内的 icon 必须来自对应 Lottie JSON，think 使用 `cloud.json`、toolcall 使用 `pen.json`、search 使用 `glass.json`，非当前行停在 JSON 第一帧，当前最新一行才播放动画，尺寸按列表状态位 24×24px。半层内标题、正文和检索结果都按正常文档流向下展开，不能使用脑雾里的绝对定位或固定高度裁切内容。`搜索景区官方网站及相关信息` 行右侧不展示叠头像；思考结束后只更新半层标题为「思考结束」，该行仍保持进行中的检索列表样式，不切换为最终结果卡。
- 参考来源列表底部数据必须和 Figma `7月方案` node `168:2894` 保持一致：第一条作者 `亦一行`，标题「新疆伊犁环线10天自驾攻略，直接抄作业！」，点赞 `2765`；第二条作者 `sailormmoon`，标题「新疆伊犁环线10天9晚 （上）」，点赞 `234`；第三条作者 `小k`，标题「记录一下伊犁环线自驾详细路线」，点赞 `1634`；第四条作者 `Oolong`，标题「二刷新疆伊犁9天治愈环线攻略」，点赞 `67`；第五条作者 `橘子汽水`，标题「新疆伊犁9天自驾🚗懒人不赶路天花板路线」，点赞 `3452`。网页来源展示「知乎：新疆自驾路线汇总（新疆自驾行必读攻略）」和「携程：自驾新疆，应如何规划线路？」。
- response 状态行打开的「思考过程」半层里，`搜索景区官方网站及相关信息` 类目展开后仍使用前面对话流中的检索列表样式，不展示右侧叠头像，也不切换为结束态结果卡。
- 思考/来源半层上滑后参考 Figma `think 方案` node `12:1153` 进入高半层状态，高度从 639px 柔和过渡到 798px；不是重新打开第二个弹层。拖拽把手、半层内容向上移动，或在半层内容区产生向下浏览的滚动意图时，应在手势过程中立即切换为高半层，不等松手后才变化。半层向下拖动超过 96px 时直接关闭半层，不先退回低半层。半层打开时所有 think/toolcall 类目默认收起；单个类目支持点击展开/收起，多个类目可以同时展开，点击另一个类目不能自动收起前一个类目；点击行本身不能改变半层高度。
- 半层类目点击展开内容时，正文区域不能硬切出现；内容区使用“呼吸展开”：从 `max-height: 0` 到内容高度柔和展开，同步 `opacity 0 -> 1`，不做上下位移，时长 400ms，曲线 `cubic-bezier(0.22, 0.61, 0.36, 1)`。
- 半层过程列表的展开能力由真实内容决定：只有存在正文、检索结果或来源结果的类目才显示右侧 chevron 并支持点击展开；当前 `thinkCompact` 已按 Figma `7月方案` node `168:2894` 补充正文，因此需要支持展开，不再作为纯标题静态节点处理。
- 半层里的思考过程数据必须和前面真实流式链路对应：`think`「规划新疆伊犁红圈10天行程」对应第一段深度思考正文；`toolcall`「制定新疆伊犁环线10日自驾游路线」对应官方检索正文；`thinkCompact`「查找景区开放信息和路线攻略」对应第二段思考正文；`toolcallSearch`「搜索景区官方网站及相关信息」为无正文阶段，只展示标题和状态；`thinkPlan`「考虑提供合理的行车距离和交通建议」对应最终行程规划思考。不得为了半层单独编一套和对话流无关的数据。
- response 正文完整输出后进入 complete 态时，页面保持当前阅读位置，不自动跳到回答结尾；只有点击“至底”悬浮按钮才滚动到回答底部。
- think 和 toolcall 必须连贯在同一条脑雾里出：上一个阶段正文输出完成后停顿 1s，再切到下一个阶段。阶段切换时禁止把上一阶段长文本和下一阶段短文本叠在一起做过渡，也禁止按每个字符持续拉伸脑雾背景；这两种都会造成文本和脑雾背景长度不匹配或出现卡顿。切换瞬间应只保留一套当前文本和一套当前背景：旧阶段结束后清空标题/正文，下一阶段从空文本开始流式输出，脑雾使用当前阶段稳定宽度（常规 361px，紧凑 232px）。阶段切换不做 blur、缩放或残影叠层，只允许当前内容轻淡入；轻浮现位移控制在 1px 内，背景叠加 220ms 渐隐渐显，避免背景弹跳或大幅拉伸。该效果只作用于左侧手机里的真实 `think -> tool call -> think` 阶段切换，不改变数据顺序、进度条节点或 response 样式。
- think / toolcall 正文在流式输出时必须使用左对齐，禁止 `text-align: justify`。两端对齐会在字符追加和换行时重新分配字间距，导致内容间距抖动。
- 所有脑雾背景展开必须是缓慢连续的缓动，不允许随字符或行数一格一格跳动；横向宽度变化使用柔和曲线 `cubic-bezier(0.16, 0.84, 0.32, 1)`，纵向高度由正常文档流内容自然撑开，不用固定高度兜底。
- 判断动画结束后，同一位置替换为 Dots 回复气泡，文本按字符连续打出，形成完整 typewriter 动作；不显示硬光标、尾点或闪烁描边。
- 底部输入区固定在页面底部，输入区背后使用独立透明渐变遮罩层：渐变层允许向左右和底部外扩，并由最外层手机容器统一裁切，避免内层背景露出额外圆角；渐变使用 `#F5F5F5` 三段 stop：`0% / 0%`、`18% / 80%`、`36% / 100%`，36% 后保持 100% 到底部；底部渐变底座不使用背景模糊；输入框、AI 标注和 home indicator 必须位于该遮罩层之上。
- 输入框高度 48px，左右 margin 12px，padding 12px 14px，圆角 16px，背景纯色 `#FCFCFC`，阴影 `0 0 36px rgba(0,0,0,0.08)`，输入框自身保留 25px backdrop blur 的合成层。输入框父级不能使用 `isolation: isolate` 这类隔离合成上下文，否则生产浏览器可能只采样到底部渐变层，导致线上看不到对话内容的背景模糊。
- 占位文案 16px / 24px，颜色 `Description`；右侧相机和添加入口间距 16px。
- response 内容超出当前可视区域且用户不在回答底部时，显示 Figma `think 方案` node `116:4499` 的“至底”悬浮按钮：36×36px，位于输入区上方，right 12px / bottom 108px，白色 40% 背景，圆角 100px，`0 0 36px rgba(0,0,0,0.08)` 阴影，内部使用 `descending_m` 18×18 图标。点击后平滑滚动到回答底部并隐藏；悬浮按钮本体和箭头不做入场、按压或额外点击动画；按钮不能放进输入框内部，也不能跟随内容列表滚动。
- Demo 和 pattern 页面必须复用 `DottedDemoScreen` 作为页面模板。说明面板可以在手机画板外侧，但不得重建另一套状态栏、背景、输入栏或气泡。
- `conversation-streaming` 项目 demo 当前用于动作验证，页面右侧只保留流程进度控制器：标题「回答loading新增长思考模式」放在进度条上方，使用首页展示字体栈 `var(--font-ui)`，22px / 30px / Semibold，居中。标题下方不展示流式输出切换控件，流式输出仅保留 B 版；B 版参考 Claude 输出观感做尾部透明度流式：应用范围包括判断文案、context 回答气泡、think/toolcall 脑雾和最终 response，其中 context 采用更快节奏和更轻位移。B 版规则是：文本仍按真实字符流进入文档流，不使用 Canvas 或白色遮罩；正在输出时只有最后约 10 个字符进入透明度梯度，最后一个字符约 34% 透明度，向前逐步过渡到 100%，每次新字符进入时旧尾部字符通过 260ms ease-out 逐步变实；输出完成时不能直接跳到 100%，而是保留尾部梯度并用 760ms 柔和动画慢慢回到 100% 透明度。think/toolcall 中标题和正文分阶段处理：标题尾部透明度完成后，正文或检索结果再用 180ms 延迟淡入进入，避免标题和内容同时抢焦点。该方案用于减少逐字跳出感，同时避免遮罩覆盖整段内容造成文本和背景错位。控制器不使用卡片背景、描边或阴影，也不显示顶部编号/当前状态标题行；进度条包含 10 个可点击节点，顺序为「判断 / think / context / think / tool call / think / tool call / think / response / 完成」，其中 context 是第一条回答气泡；已完成进度必须准确推进到当前节点圆心。点击节点直接跳到对应 demo 状态，并从该节点继续自动播放后续内容，不停在单个节点。初始状态只显示小尺寸主按钮「开始演示」；点击后从头不间断自动播放，并切换为两个小按钮：「暂停」为主按钮，「重新开始」为 36×36px 次级图标按钮，图标使用 ReDs Icons / `refresh_b`。点击暂停后必须冻结当前帧，不重置到节点开头，主按钮文案改为「继续」；点击继续后从当前流式位置恢复播放。点击重新开始后从头继续自动播放。进度条和按钮都在控制区域内水平居中；当页面进入上下布局时，手机页面、进度条和按钮必须共用同一条居中轴线，整体在可用视口内垂直居中，不保留额外底部偏移。右侧大段说明、标签、旧的「分步骤 / 一口气出完」按钮和下方状态说明模块先不展示。
- 思考过程展示方式单独提供「单条切换 / 连续堆叠」两个方案，不互相覆盖。连续堆叠方案严格参考 Figma `7月方案` node `168:2641`、`187:4125` 以及 `think 方案` node `157:6665`：所有已经出现过的思考过程都保留在同一个脑雾模块内，新过程追加在下方；上一过程进入收起态，只保留标题行和状态 icon，详情内容按半层展开/收起同类动效从当前高度缓动收至 0，不允许硬切消失。连续堆叠脑雾顶部固定展示「正在思考」入口，内容容器相对脑雾顶部 y=12，入口高 36px，入口文字从内容容器内 x=8 / y=8 开始；文案 14px / 20px、`Description` 色、Regular。只要当前思考标题或正文未输出完成，入口文案持续低对比慢速扫光，使用固定文字底色叠加单条独立光带，光带约 68px 宽，每轮从左往右扫过约 2.8s，扫完整段文字后停顿 2s 再开始下一轮，输出完成后停止扫光并恢复普通文字；右侧箭头复用思考完成状态行同款 14×14px 箭头，直接跟在文案后方，间距 4px，不能贴到最右侧；入口始终固定在思考条目上方，不参与条目滚动，点击行为沿用思考脑雾整体点击打开半层。脑雾背景必须在外层容器绘制，滚动只能发生在入口下方的条目内容层，避免 `overflow` 裁掉外扩 blur；连续堆叠条目滚动层最大高度 154px，超过后只在思考区域内部滚动，不继续撑高对话流；正在输出的当前内容必须完整展示，内容输出过程中滚动区使用 160ms 线性 `requestAnimationFrame` 补间跟随贴底，避免直接跳动和 smooth 动画叠加抖动；滚动区顶部渐变蒙层只在内容已经发生内部滚动时出现，用 15px 渐变柔和遮住已滚出的旧内容；未发生滚动时不显示蒙层，底部不做渐隐以保证当前输出完整可读。脑雾背景圆角 22px，blur 8px，透明度 90%，背景相对内容顶部/左右外扩 6px / 10px，下边距文字内容外扩 20px。连续堆叠条目 icon 为 32×32px，位于内容容器左侧，标题从 x=40 开始，14px / 20px、Regular；当前正在思考项使用 `Paragraph` 色，已完成项与收起项使用 `Description` 色。每一项标题与正文全部输出完成后，必须先把该项详情内容按收起动画从当前高度缓动收至 0，收起完成后左侧状态再从对应 Lottie/emoji 切换为 Figma `7月方案` node `187:4098` 导出的完成态 SVG 资源：32×32px，包含浅灰圆形底、0.4px 弱描边圆环和 1.5px 弱勾线；资源本身不带下方虚线连接，不能用 CSS 手画的深色大对勾替代。Lottie 与完成态对勾切换必须发生在详情收起之后，并使用原地溶解：Lottie 轻微 blur 淡出，对勾以 blur 2px 到清晰的方式淡入，不做明显缩放或位移；过渡时长控制在 640-680ms，避免硬切和弹跳感。连接线由行级状态单独控制：完成收起行之间保留行级虚线连接，连接线使用固定短段连接相邻状态圆，距离上下圆形本体各保留 2px 间距，不能依赖收起行高度计算；最后一个节点、当前展开正文下方、以及任何详情展开行都不能继续出现连接线，正文左侧不能继续出现连接线。正文左侧同标题 x=40，宽 305px，12px / 18px。标题行之间不额外拉开大间距，只保留状态 icon 形成的自然节奏。当前最新过程展开详情并继续流式输出，详情外层打开后使用稳定的最大高度上限，内容按正常文档流自然增长，禁止按每个字符进度反复重算 `max-height`，避免滚动位置和高度动画互相抢占造成上下跳动；脑雾背景必须由堆叠内容自身生成并跟随内容高度，而不是沿用外层 thinking 容器背景，避免文本和背景错位。没有正文的紧凑 think 不渲染空详情容器，也不能给脑雾底部留下额外空白。当前展开行左侧必须显示 1px 竖向虚线，位置跟随状态 icon 中心，但连接线必须避开 Lottie / 对勾图形本体，从图形底部之后开始，不能穿过图形；收起行之间保留虚线连接。连续堆叠方案的脑雾宽度固定 361px，不随紧凑 think 收窄；回答结束前，思考脑雾先收束为完成态，再在同一对话流位置演变为最终 response 卡片。
- 第二个 tool call「搜索景区官方网站及相关信息」支持「工具调用的笔记展示样式」A/B 方案切换：A 方案为里外一致，对话流里的检索行和半层详情都使用同一组轻量文本行；B 方案严格参考 Figma `7月方案` node `168:2894`，对话流外部只展示「小红书笔记 / 携程网 / 马蜂窝」三枚来源 pill，三枚 pill 必须随检索流式进度逐个出现，不能一次性闪现；出现动效使用组合方案：整组保持同一行归位感，单个 pill 以 80ms 错峰依次进入，使用轻微左向位移、低强度 blur 还原和透明度淡入，不做弹性缩放。半层详情展开时展示带缩略图、作者、点赞数的详细笔记卡与网页来源行。B 方案默认选中，用来验证“外部降噪、详情承载完整信息”的展示策略。
- 点击顶部导航「项目demo」后进入沉浸式项目 demo 页面，不使用 docs 普通顶部导航、侧栏或页面框架。页面右上角固定显示关闭入口，点击后返回 `/docs` 主页。
- `conversation-streaming` 项目 demo 的左右两列作为一组居中展示，最大宽 1062px；左右列间距为 72px，手机画板和右侧流程控制器都保持各自居中，避免在大屏上被剩余空间拉散。
- `conversation-streaming` 项目 demo 的手机画板外层容器不加投影，只保留圆角裁切，避免沉浸式页面里出现额外浮层感。
- B 版尾部透明度流式不叠加旧的标题扫光透明文字样式。B 版标题、判断文案和正文都由字符透明度负责视觉过渡，避免父级 `color: transparent` 把逐字内容隐藏，造成标题区域空白。
- 项目 demo 页面作为独立汇报面板，不显示 docs 通用上一页/下一页页脚导航。

## 切图清单

当前代码接入 `src/assets/dotted/` 下的 `think 方案` 切图资源：

| 资源名 | 用途 |
|--------|------|
| `think-back.svg` | 顶部返回 |
| `think-user-avatar.svg` | 导航头像 |
| `think-more.svg` | 顶部更多 |
| `think-status-cellular.svg` | 状态栏信号 |
| `think-status-wifi.svg` | 状态栏 Wi-Fi |
| `think-status-cap.svg` | 状态栏电池帽 |
| `think-message-voice.svg` | 输入框左侧语音入口 |
| `think-camera.svg` | 输入框相机入口 |
| `think-add-circle.svg` | 输入框添加入口 |
| `think-user-bubble-line.svg` | 用户气泡弱分隔线备用切图 |
| `think-lottie/cloud.json` | 判断过程和 think 过程 Lottie |
| `think-lottie/pen.json` | toolcall 过程 Lottie |
| `think-lottie/glass.json` | 第二段 toolcall 检索过程 Lottie |
| `think-response-avatar-1.png` / `think-response-avatar-2.png` | response 状态行头像 |
| `think-response-arrow.svg` | response 状态行右箭头 |
| `think-response-copy.svg` / `think-response-share.svg` / `think-response-dislike.svg` / `think-response-refresh.svg` | response 底部操作栏图标 |
| `think-descending.svg` | response 长内容快速跳到回答底部 |
| `source-logo-yt.png` / `source-logo-sailormmoon.png` / `source-logo-alice.png` / `source-logo-breeze.png` | 思考/来源半层里的来源头像 |
| `source-image-1.png` / `source-image-2.png` / `source-image-3.png` / `source-image-sailormmoon-1.png` / `source-image-sailormmoon-2.png` / `source-image-sailormmoon-3.png` | 思考/来源半层里的笔记图片 |
| `source-quote.svg` / `source-like.svg` | 思考/来源半层里的引用和点赞图标 |
| `think-lottie/eyes.json` / `book.json` / `doc.json` / `bulb.json` / `star.json` | 待逐个确认后接入的后续思考阶段 Lottie |
| `think-lottie/preview.json` | Lottie 总览预览，不在产品态同时播放 |

## 触觉反馈

| 交互 | 意图 |
|------|------|
| 点击返回/更多 | light |
| 点击语音入口 | light |
| 按住输入框语音 | medium |
| 点击相机/添加 | light |
| 点击至底按钮 | light |
| 点击 response 状态行 | light |
| 发送新消息 | success |

## 依赖组件

| 组件 | 状态 | 用途 |
|------|------|------|
| DottedDemoScreen | 已实现 | 当前点点页面模板和 pattern demo 基座 |
| DotsMessageBubble | 已实现 | User / Dots 文本消息气泡统一样式 |
| Pattern 页面 | 已实现 | 时间戳、思考态、流式输出等能力说明 |

## 多宽度适配

| 宽度 | 规则 |
|------|------|
| Mobile 393px | 规范画板规格 |
| Tablet | 保持手机画板预览，不拉伸页面 |
| Desktop | 保持手机画板预览，不拉伸页面 |
